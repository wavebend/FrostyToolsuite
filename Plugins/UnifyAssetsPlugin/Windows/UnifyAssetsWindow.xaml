<ctrl:FrostyDockableWindow x:Class="UnifyAssetsPlugin.Windows.UnifyAssetsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrl="clr-namespace:Frosty.Controls;assembly=FrostyControls"
        mc:Ignorable="d"
        Title="Unify Assets" Height="300" Width="350" ResizeMode="NoResize" Icon="/UnifyAssetsPlugin;component/Icons/UnifyAssets.ico" WindowStartupLocation="CenterScreen" SnapsToDevicePixels="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FrostyControls;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid Background="{StaticResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="38"/>
        </Grid.RowDefinitions>

        <Grid Background="{StaticResource ListBackground}">
            <DockPanel LastChildFill="True">
                <Grid DockPanel.Dock="Top" Margin="5">
                    <StackPanel>
                        <DockPanel LastChildFill="True" Margin="0,0,0,5">
                            <Label HorizontalAlignment="Center" FontFamily="Global User Interface">
                                <AccessText x:Name="LinkedAssetsAccessText" TextAlignment="Center" TextWrapping="WrapWithOverflow" Text=""/>
                            </Label>
                        </DockPanel>
                        <DockPanel LastChildFill="True" Margin="0,0,0,5">
                            <ListBox x:Name="LinkedAssetsListBox" VerticalContentAlignment="Center" Height="135" Margin="1" BorderThickness="1" BorderBrush="#454545" Background="#141414"/>  
                        </DockPanel>
                        <DockPanel LastChildFill="True" Margin="0,0,0,10">
                            <Label HorizontalAlignment="Center" FontFamily="Global User Interface">
                                <AccessText TextAlignment="Center" TextWrapping="WrapWithOverflow" Text="Do you wish to make these assets compatible with both versions of the game?"/>
                            </Label>
                        </DockPanel>
                    </StackPanel>
                </Grid>
            </DockPanel>
        </Grid>

        <Border Grid.Row="1">
            <Grid Margin="8">
                <DockPanel LastChildFill="False">
                    <Button Content="No" DockPanel.Dock="Left" Width="75" Click="noButton_Click"/>
                    <Button Content="Yes" DockPanel.Dock="Right" Width="75" Click="yesButton_Click"/>
                </DockPanel>
            </Grid>
        </Border>
    </Grid>
</ctrl:FrostyDockableWindow>
