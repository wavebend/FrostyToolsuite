<ctrl:FrostyDockableWindow x:Class="FrostyModManager.SeparatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FrostyModManager.Windows"
        xmlns:ctrl="clr-namespace:Frosty.Controls;assembly=FrostyControls"
        xmlns:core="clr-namespace:Frosty.Core.Controls;assembly=FrostyCore"
        mc:Ignorable="d"
        Title="Add Separator" Height="110" Width="300" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FrostyControls;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid Background="{StaticResource ListBackground}">
            <Grid.RowDefinitions>
                <RowDefinition Height="52"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Vertical" Margin="4,7,4,4" VerticalAlignment="Top">
                <ctrl:FrostyWatermarkTextBox x:Name="nameSeparatorTextBox" WatermarkText="Name" BorderThickness="1" VerticalContentAlignment="Center" Height="22" TextChanged="nameSeparatorTextBox_TextChanged"/>
                <TextBlock x:Name="charsTextBox" Foreground="{StaticResource FontColor}" VerticalAlignment="Center" Height="16" Margin="0,4,0,0" Text=""/>
            </StackPanel>

            <Grid Grid.Row="1" Margin="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" x:Name="cancelButton" Content="Cancel" Width="50" Height="22" HorizontalAlignment="Left" Click="cancelButton_Click"/>
                <Button Grid.Column="1" x:Name="doneButton" Content="OK" Width="50" Height="22" HorizontalAlignment="Right" Click="doneButton_Click"/>
            </Grid>
        </Grid>
    </Grid>
</ctrl:FrostyDockableWindow>
